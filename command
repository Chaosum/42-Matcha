docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/init.sql" && docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/seed.sql" && docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/sp_App.sql" && docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/sp_Auth.sql" && docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/sp_Checks.sql" && docker exec -it database bash -c "mysql -u root -ppassword db < /docker-entrypoint-initdb.d/sp_users.sql"
